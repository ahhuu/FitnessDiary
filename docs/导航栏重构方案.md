# App UI 专项优化：底部导航栏重构

**指令背景：**
目前的底部导航栏图标风格混乱（例如“饮食”使用了“编辑/笔”的图标，语义不明），且交互反馈单一。用户希望采用行业标准的视觉交互：**默认状态显示线性轮廓（Outline），选中状态显示实心填充（Filled）。**

**核心目标：**
1.  **图标重绘：** 替换现有的 4 个导航图标，确保线条粗细、圆角风格统一。
2.  **语义修正：** 将“计划”和“饮食”图标分别修正为app\src\main\res\drawable\img_welcome_hero.webp这个插画上图标的哑铃和叶子（它们正好是线框图标），其它图标重绘保持风格一致。
3.  **状态切换：** 实现“线框 -> 实心”的点击切换效果。
4.  **配色统一：** 选中时使用 App 主题色，未选中时使用灰色。

---

### 📋 详细任务清单

#### 1. 资源准备 (Vector Assets)
我们需要准备 4 组图标，每组包含两个状态（Outline 和 Filled）。请生成或引入以下 Vector Drawable (`res/drawable`)：

* **打卡 (Check-in):**
    * `ic_nav_checkin_outline.xml`: 日历/打卡表的线框图。
    * `ic_nav_checkin_filled.xml`: 日历/打卡表的实心图。
* **计划 (Plan):**
    * `ic_nav_plan_outline.xml`: 哑铃的线框图（根据app\src\main\res\drawable\img_welcome_hero.webp这个插画）。
    * `ic_nav_plan_filled.xml`: 哑铃的实心图。
* **饮食 (Diet) - *需修正语义*:**
    * `ic_nav_diet_outline.xml`: 叶子的线框图（根据app\src\main\res\drawable\img_welcome_hero.webp这个插画）。
    * `ic_nav_diet_filled.xml`: 叶子的实心图。
* **我的 (Profile):**
    * `ic_nav_profile_outline.xml`: 用户头像/半身像的线框图。
    * `ic_nav_profile_filled.xml`: 用户头像/半身像的实心图。

#### 2. 创建状态选择器 (Selectors)
在 `res/drawable` 目录下创建 4 个选择器文件，用于逻辑判断：
* `selector_nav_icon_checkin.xml`
* `selector_nav_icon_plan.xml`
* `selector_nav_icon_diet.xml`
* `selector_nav_icon_profile.xml`


3. 创建颜色选择器 (Color State List)
在 res/color 目录下创建 selector_nav_color.xml，用于控制文字和图标的着色：

XML
<selector xmlns:android="[http://schemas.android.com/apk/res/android](http://schemas.android.com/apk/res/android)">
    <item android:color="?attr/colorPrimary" android:state_checked="true"/>
    <item android:color="?attr/colorOnSurfaceVariant"/>
</selector>
4. 菜单文件更新 (res/menu/bottom_nav_menu.xml)
将 android:icon 属性指向刚才创建的 selector 文件，而不是具体的 png/xml。

XML
<menu xmlns:android="[http://schemas.android.com/apk/res/android](http://schemas.android.com/apk/res/android)">
    <item
        android:id="@+id/nav_checkin"
        android:icon="@drawable/selector_nav_icon_checkin" 
        android:title="打卡" />
    
    <item
        android:id="@+id/nav_plan"
        android:icon="@drawable/selector_nav_icon_plan"
        android:title="计划" />

    <item
        android:id="@+id/nav_diet"
        android:icon="@drawable/selector_nav_icon_diet"
        android:title="饮食" />

    <item
        android:id="@+id/nav_profile"
        android:icon="@drawable/selector_nav_icon_profile"
        android:title="我的" />
</menu>
5. 布局属性调整 (activity_main.xml)
在 BottomNavigationView 中应用颜色选择器，并调整 Material 3 的默认样式以符合“透明背景”的需求。

XML
<com.google.android.material.bottomnavigation.BottomNavigationView
    ...
    app:itemIconTint="@color/selector_nav_color"
    app:itemTextColor="@color/selector_nav_color"
    app:labelVisibilityMode="labeled"
    
    app:itemActiveIndicatorStyle="@style/App.Custom.Indicator" 
    />
💡 风格备注 (Style Note)
Icon Style: 线条宽度建议统一为 1.5dp - 2dp，圆角风格 (Rounded Corners)，保持视觉的柔和感，与 App 整体的莫兰迪色系搭配。