# 2026-07-01 开发日志

## 完成事项

1. **入口按钮集成**：
   - 设计了 `ic_book.xml` 作为动作库专属矢量图图标。
   - 修改 `fragment_plan.xml`，在计划页面的哑铃图标和设置图标中间插入动作库入口 ImageButton。
   - 修改 `PlanFragment.java` 绑定该按钮的点击路由跳转逻辑。
   - 在 `nav_graph.xml` 中成功注册动作库页面的导航节点。

2. **动作库核心页面交互与器械过滤升级**：
   - 实现了双栏式主交互布局：左侧肌肉群大类侧边栏，右侧为对应的动作网格列表。
   - **优化**：左侧部位导航升级为**大类-子类伸缩折叠列表结构**。支持点击“胸/背/腿/肩/臂/腹”大类展开或闭合子部位（如上胸、中下胸等），并默认高亮当前激活的细分部位。
   - **优化**：顶部器械快速筛选栏补齐了 **“拉力器”** 与 **“固定器械”** 的 Filter Chips。
   - **优化**：内存动作过滤算法升级，增加了对拉力器与固定器械过滤分支，并且能根据动作的名称 and 要领描述，将所有动作智能、精准地归纳映射入相应的子部位分类中。

3. **视觉卡片与动作要领指南深度打磨**：
   - 动作列表卡片采用 `@color/plan_blue_container` 淡蓝配色，若无图时取动作首字为圆形徽标艺术字，配以“讲解”Badge。
   - **优化**：创建了 [ExerciseGuideHelper.java](file:///d:/code/JaProject/FitnessDiary/app/src/main/java/com/cz/fitnessdiary/utils/ExerciseGuideHelper.java) 指南生成工具。针对内置核心动作（卧推、硬拉、弯举等）定制了起跑姿势、发力轨迹、呼吸与核心建议等专业文案，并针对各大肌群部位实现智能生成兜底指南。
   - **优化**：[ExerciseDetailBottomSheet.java](file:///d:/code/JaProject/FitnessDiary/app/src/main/java/com/cz/fitnessdiary/ui/fragment/ExerciseDetailBottomSheet.java) 接入该助手类，点击任何卡片弹出的 BottomSheet 均展示极高质量、详尽清楚的指南。

4. **列表卡片动图直显与详情白板 Bug 修复**：
   - **卡片直显动图**：在卡片中加入了动图 ImageView，列表上如果配有 3D GIF 动图，将在线直接播放，让列表生动呈现。
   - **详情空白椭圆修复**：查明并消成了 Glide 的 `placeholder` 和 `error` 的图层遮挡，做到了在下载前完美展示动作大字占位、下载成功后淡出并无缝覆盖 GIF 的过渡效果。

5. **网络鲁棒性与 Lottie Crash 修复**：
   - **Bug 修复**：针对 Lottie 从网络拉取 JSON 时，国内直连被拒（返回 403 错误）导致未捕获 `IllegalStateException` 并闪退的问题，在 `setAnimationFromUrl` 之前配置了 `setFailureListener`。

6. **本地离线 3D GIF 动图全量覆盖重构**：
   - **全量下载**：读取内置动作的 [exercise_library.json](file:///d:/code/JaProject/FitnessDiary/app/src/main/assets/exercise_library.json) 索引，为全库 **120 多个预置动作** 精确下载了 **78 个核心主流动作的 3D GIF**（如卧推、硬拉、弯举等），直接以**动作的中文名称**（如 `哑铃上斜卧推.gif`）保存在本地的 `app/src/main/assets/gifs/` 目录下。并在同目录下打包了 `胸部.gif`、`腿部.gif` 等 6 大部位的离线兜底图。
   - **英文资源中文同步化（新增）**：将第一次下载的 20 个英文文件（如 `pushups.gif`、`pullups.gif`、`crunches.gif` 等）重命名同步为中文名（`标准俯卧撑.gif`、`引体向上.gif`、`卷腹.gif`），并删除了冗余的英文源文件。
   - **空格连字符匹配 Bug 修复（新增）**：修正了中英文模糊匹配过程中，因为连字符（如 `push-up`）与空格（如 `push up`）判定不一导致的匹配缺失。纠正后一并补齐了 `宽距引体向上`、`下斜俯卧撑`、`上斜俯卧撑`、`窄距俯卧撑`等核心动作。
   - **读取架构升级**：彻底删除了原先的 100 多行中英文硬编码 Mapping 映射代码。重写 `ExerciseCardAdapter` 和 `ExerciseDetailBottomSheet` 的 Glide 读取机制：动态检测本地 Assets 目录下是否存在以动作中文名命名的 `gif` 文件，存在则秒级载入本地文件进行播放，不存在（详情中）则自动以所属肌肉部位（如 `胸部.gif`）进行兜底播放，完全免除网络开销，100% 离线秒开！

7. **编译与验证**：
   - 运行项目整包编译 `.\gradlew.bat assembleDebug`，编译顺利通过（**BUILD SUCCESSFUL in 10s**）。
   - 同步更新了 `CLAUDE.md` 和 `README.md` 中的核心功能介绍。

---

## 待办事项

1. 收集和丰富更多运动大类的内置要领描述，细化 120 多个预置动作的数据质量。
2. 结合未来需求，在详情 BottomSheet 内提供“开始这个训练”的打卡按钮。
