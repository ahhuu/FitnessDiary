# 2026-07-15 详情页 AI 记录调整

## 完成事项

- 移除运动详情页新增的 AI 生成训练记录入口，保留现有“添加额外动作”，避免与原有记录流程重复。
- 饮食详情页的“AI 记录饮食”文字入口先查询本地饮食库；匹配成功时直接生成可编辑草稿，不调用 AI。
- 本地饮食库没有匹配项时才调用 DeepSeek 文本估算，并继续沿用数量、单位、营养基准和待确认规则。
- 文本识别采用组合匹配，支持将“咖喱鸡排饭”拆分为咖喱、鸡排和米饭等完整食物项，减少残余文本和不必要的 AI 调用。
- 确认页改为总营养汇总与紧凑食物卡片，食物项统一按克数编辑，并补充脂肪数据与明确的营养字段标题。
- 新增食物先搜索本地食物库；未匹配时由用户主动选择 AI 估算或手动填写，营养数据为空时不允许保存，手动数据可同步回食物库。
- 将“AI 记录饮食”入口及文字、图片、扫码选项统一为饮食页暖橙色主题，移除 Material 默认紫色 TonalButton 背景。

## 验证

- `lintDebug`、`testDebugUnitTest`、`assembleDebug` 与 `git diff --check` 已通过。

## 2026-07-15 后续调整

- 运动详情页“全部完成/全部撤销”和“添加动作”统一使用训练页蓝色主题按钮，避免继续使用默认 Tonal 紫色。
- MiMo 图片识别增加本地用户级显示计数：默认 5 次；同一图片命中最近识别缓存时不重复计数，显式重新识别才会再次计数。该计数不上传云端，也不依赖会员码。
- 当前 AI 使用个人本地模式，DeepSeek/MiMo 密钥仅从本机 `local.properties` 注入私有 APK；该 APK 禁止对外分享。
- 私教思考过程气泡改为 AI 蓝色容器色，设置页增加“显示私教思考过程”开关，默认关闭；关闭时不丢弃思考数据，且无正文回复显示明确兜底提示。
- 将该开关从系统通用设置列表移入“显示与单位设置”弹窗，集中管理显示相关选项，保留默认关闭和 SharedPreferences 持久化。
- 修复 minSdk 26 的 Android 兼容性 lint：API 28 阴影属性移入 `values-v28`，三个图片 tint 改为 `app:tint`，摄像头硬件声明改为非必需。
