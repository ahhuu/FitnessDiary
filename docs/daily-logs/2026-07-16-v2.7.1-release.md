# 2026-07-16 AI 识图修复记录（并入 v2.8.0）

## 本次变更

- 修复饮食图片识别对 JSON、代码块、`<action>` 包装和多模态文本响应的兼容性问题。
- 私教页面把结构化食物结果转换为正常中文消息，内部 JSON 只保留给动作解析，不直接展示给用户。
- 移除 MiMo 和 DeepSeek 的 App 侧 completion token 上限，以及文本聊天历史和消息截断。
- 取消两个图片入口的最长边缩放，完整解码并按高质量图片发送，避免小份食物因缩放或压缩丢失。
- 增加 `FoodActionParser` 单元测试，覆盖代码块、动作标签、转义 JSON、数组和回复字段。

## 验证

- `:app:testDebugUnitTest`：通过
- `:app:assembleDebug`：通过
- `git diff --check`：通过

## 发布归属

本记录中的 AI 识图修复、完整图片请求和私教消息展示调整，未单独发布 v2.7.1，最终与 7 月 18、19 日功能变更合并到 v2.8.0。
